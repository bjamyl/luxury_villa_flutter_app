import 'package:flutter/material.dart';

class NearScreen extends StatelessWidget {
  const NearScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('The Near You Screen'));
  }
}
